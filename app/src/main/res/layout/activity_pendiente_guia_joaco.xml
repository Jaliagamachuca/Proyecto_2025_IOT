<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#FFFFFF"
    android:padding="24dp"
    android:gravity="center">

    <!-- Ícono de pendiente -->
    <TextView
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:text="⏱"
        android:textSize="48sp"
        android:gravity="center"
        android:background="#000000"
        android:textColor="#FFFFFF"
        android:layout_marginBottom="32dp" />

    <!-- Título principal -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Cuenta pendiente de aprobación"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="#000000"
        android:gravity="center"
        android:layout_marginBottom="24dp" />

    <!-- Descripción -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="El administrador debe revisar tu perfil antes de que puedas comenzar a usar la app como guía."
        android:textSize="16sp"
        android:textColor="#666666"
        android:gravity="center"
        android:layout_marginBottom="16dp"
        android:lineSpacingExtra="4dp" />

    <!-- Texto adicional -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Te avisaremos por correo y notificación cuando tu cuenta esté habilitada."
        android:textSize="14sp"
        android:textColor="#999999"
        android:gravity="center"
        android:layout_marginBottom="80dp"
        android:lineSpacingExtra="4dp" />

    <!-- Botón cerrar sesión -->
    <Button
        android:id="@+id/btn_cerrar_sesion"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:text="Cerrar sesión"
        android:textColor="#FFFFFF"
        android:backgroundTint="#1976D2"
        android:textSize="16sp"
        android:textStyle="bold"
        android:textAllCaps="false" />

</LinearLayout>